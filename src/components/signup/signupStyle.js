import { makeStyles } from "@mui/styles";

const signupStyles = makeStyles({
	signupPage: {
		height: '100vh',
		display: 'flex'
	},

	signupButton: {
		borderRadius: '20',
		color: '#07539F',
	},

	formContainer: {
		margin: '10% auto',
		alignContent: 'center',
		textAlign: 'center',
	},

})

export default signupStyles